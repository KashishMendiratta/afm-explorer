"""MCP server exposing the AFM Explorer backend's REST API as tools for an
LLM agent (Claude Desktop, a LangGraph agent, etc.) — see server.py for the
tool definitions and this package's section of the repo README for setup.
"""
